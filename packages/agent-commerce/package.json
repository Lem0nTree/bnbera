{
  "name": "@bnbera/agent-commerce",
  "version": "0.1.0",
  "private": true,
  "description": "ERC-8183 job commerce contracts and fail-closed lifecycle boundary for BNBEra",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "Remove-Item -Recurse -Force dist -ErrorAction SilentlyContinue",
    "lint": "eslint src test --max-warnings 0",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@bnbera/domain": "workspace:*",
    "@altananetwork/sdk": "0.9.0",
    "viem": "2.56.3",
    "zod": "3.24.1"
  }
}

# Active MVP documentation

Read only the documents relevant to your assignment:

- [Master plan](MVP-MASTER-PLAN.md): product scope, current implementation and four milestone gates.
- [Tasks](MVP-TASKS.md): nine concrete tasks and two-agent execution order.
- [Runbook](MVP-RUNBOOK.md): existing commands, target cron behavior and runtime verification.

`config/standards.lock.json` remains the integration pin source. Authentication and donor provenance retain their focused reference documents. `release-evidence/` and `evidence/` retain test artifacts/templates consumed by tooling; their historical results are not current gate acceptance.

Superseded planning documents, ADR narratives, runbooks and handoffs are archived and excluded from default agent reading/search. The active master plan carries forward the required identity, custody and migration safeguards. Do not load the archive for normal MVP tasks.

# W0/W1 QA and release evidence — 2026-09-03

## Scope and checkout

- Agent/workstream: McClintock / A11 QA + integration.
- Assigned wave/vertical: W0–W1, QA + Deployment + Submission.
- Feature gates: Core Marketplace; QA + Deployment + Submission.
- Risk tier: R1 — stateful application read-model/API/browser verification.
- Checkout: `/home/ubuntu/bnbera-w0-w1`.
- Branch: `codex/w0-w1-marketplace`.
- Base SHA before QA edits: `ba90043b926e132d05eb21fc05ce5cb37e970341`.
- Integration/head SHA: pending coordinator commit and push.
- No commit, branch switch, reset, clean, deployment, signing, wallet use, or
  external state change was performed by this worker.

The checkout already contained uncommitted W0/W1 output. It was preserved; the
QA worker changed only the paths listed below.

## Documentation-read evidence

Read in full before the first edit:

- `/home/ubuntu/bnbera-w0-w1/AGENTS.md`
- `/home/ubuntu/bnbera-w0-w1/docs/04-bnbera-master-implementation-plan.md`
- `/home/ubuntu/bnbera-w0-w1/docs/05-subagent-delivery-plan.md`
- `/home/ubuntu/bnbera-w0-w1/config/standards.lock.json`
- `/home/ubuntu/bnbera-w0-w1/docs/01-marketplace-donor-merge-plan.md`
- `/home/ubuntu/bnbera-w0-w1/docs/02-greenfield-data-and-evidence-plan.md`
- `/home/ubuntu/bnbera-w0-w1/docs/03-no-code-agent-deployer-plan.md`
- `/home/ubuntu/bnbera-w0-w1/docs/adr/0001-altana-studio-custody-bootstrap.md`
- `/home/ubuntu/bnbera-w0-w1/docs/adr/0003-wave1-legacy-migration-repair.md`

Consumed shared schemas and interfaces:

- `/home/ubuntu/bnbera-w0-w1/packages/domain/src/constants.ts`
- `/home/ubuntu/bnbera-w0-w1/packages/domain/src/identity.ts`
- `/home/ubuntu/bnbera-w0-w1/packages/domain/src/states.ts`
- `/home/ubuntu/bnbera-w0-w1/packages/domain/src/services.ts`
- `/home/ubuntu/bnbera-w0-w1/packages/domain/src/events.ts`
- `/home/ubuntu/bnbera-w0-w1/packages/domain/src/canonical.ts`
- `/home/ubuntu/bnbera-w0-w1/packages/domain/src/index.ts`
- `/home/ubuntu/bnbera-w0-w1/packages/marketplace/src/types.ts`
- `/home/ubuntu/bnbera-w0-w1/packages/marketplace/src/eligibility.ts`
- `/home/ubuntu/bnbera-w0-w1/packages/marketplace/src/read-model.ts`
- `/home/ubuntu/bnbera-w0-w1/packages/marketplace/src/source.ts`
- `/home/ubuntu/bnbera-w0-w1/packages/marketplace/src/fixtures.ts`
- `/home/ubuntu/bnbera-w0-w1/packages/marketplace/src/index.ts`
- `/home/ubuntu/bnbera-w0-w1/packages/marketplace/src/errors.ts`
- `/home/ubuntu/bnbera-w0-w1/apps/web/src/lib/marketplace-contract.ts`
- `/home/ubuntu/bnbera-w0-w1/apps/web/app/api/marketplace/route.ts`
- `/home/ubuntu/bnbera-w0-w1/apps/web/app/api/marketplace/[slug]/route.ts`
- `/home/ubuntu/bnbera-w0-w1/packages/config/src/errors.ts`
- `/home/ubuntu/bnbera-w0-w1/packages/db/src/schema.ts`

Relevant unresolved lock constraints remain fail-closed: the standards lock is
`candidate`; the BSC 56/97 main-track decision is unresolved; ERC-8004 ABI and
bytecode verification is pending; ERC-8183 addresses/policy are unresolved;
B402 is disabled pending a paid canary; Greenfield SDK/provider selection is
pending; and Altana addresses/phase-zero acceptance are pending. Creator,
Altana activation, ERC-8183, X402/B402, Greenfield, B402, custody, and mainnet
writes are not evidenced or enabled by this QA work.

## QA changes

- `tests/vitest.config.ts` — source aliases for the cross-package QA harness.
- `tests/integration/marketplace-read-model-contracts.test.ts` — existing
  ingestion-to-marketplace projection regression coverage exercised by the
  integrated suite.
- `tests/integration/w0-w1-marketplace-contracts.test.ts` — deterministic
  marketplace contract and fixture boundary coverage.
- `tests/integration/web-marketplace-contracts.test.ts` — web adapter and
  browse/detail route contract, state, error, filter, and fail-closed tests.
- `tests/security/w0-w1-boundaries.test.ts` — static read-only route/probe and
  secret-boundary assertions.
- `scripts/verify-marketplace-api.mjs` — response-contract and fail-closed
  activation/provenance validation for the caller-supplied API endpoint.
- `scripts/verify-marketplace-browser.sh` — read-only preview route and state
  marker smoke probe with fail-closed runner/URL checks.
- `docs/release-evidence/w0-w1-qa-2026-09-03.md` — this truthful handoff.

No feature implementation, migration, infrastructure, configuration, wallet,
or production path was changed by McClintock.

## Commands and results

Passed:

- `pwd && git status --short && git branch --show-current && git rev-parse HEAD`
  — checkout `/home/ubuntu/bnbera-w0-w1`, branch
  `codex/w0-w1-marketplace`, base `ba90043b926e132d05eb21fc05ce5cb37e970341`;
  pre-existing uncommitted output was present.
- `node --check scripts/verify-marketplace-api.mjs` — pass.
- `bash -n scripts/verify-marketplace-browser.sh` — pass.
- `pnpm --filter @bnbera/domain test` — pass, 9 tests.
- `pnpm --filter @bnbera/config test` — pass, 2 tests.
- `pnpm --filter @bnbera/agent-ingestion test` — pass, 27 tests.
- `pnpm --filter @bnbera/marketplace test` — pass, 10 tests.
- `node node_modules/vitest/vitest.mjs run --config tests/vitest.config.ts
  --pool=threads --maxWorkers=1 --minWorkers=1 --reporter=dot` — 42/42;
  marketplace read-model, web adapter, W0/W1 contract, and security suites
  pass (13/13, 12/12, 10/10, 7/7).
- `pnpm --filter @bnbera/ui run build` — pass (`tsc -p tsconfig.json`).
- `pnpm --filter @bnbera/web run build` — pass (Next.js production build;
  marketplace browse/category/compare/detail and both API routes compiled).
- `BNBERA_MARKETPLACE_API_URL=http://127.0.0.1:3100/api/marketplace node
  scripts/verify-marketplace-api.mjs` against the integrated local preview —
  pass, HTTP 200 empty read contract. This is local preview HTTP evidence,
  not live marketplace, chain, payment, custody, or publication evidence.
- API probe against synthetic local HTTP empty and fixture-shaped JSON — pass;
  this verifies the probe/parser only and is not live marketplace evidence.
- API probe without `BNBERA_MARKETPLACE_API_URL` — exit 2, expected explicit
  seam defect.
- Browser probe with the local preview URL — exit 2 because `agent-browser` is
  not installed; this is an explicit evidence limitation, not a browser pass.

Known non-pass/limited results:

- `pnpm --filter @bnbera/web test` — exits 1 because the web package has no
  package-local test files; cross-package web behavior is covered by the QA
  suite under `tests/`.
- `pnpm ops:standards-check` — blocked in the earlier restricted sandbox by
  `tsx` IPC pipe `EPERM`; rerun in an environment permitting the checker’s
  temporary IPC pipe.
- Browser verification was not produced because the `agent-browser`
  executable is unavailable. The browser probe is intentionally fail-closed
  until the isolated browser runner and configured state-marker checks are
  supplied.

## Findings and release disposition

1. **DQA-005 — resolved:** the integrated source projection now passes the
   seeded ingestion-to-marketplace read-model contract. The full cross-package
   QA run is 42/42, including the expected healthy projection and complete
   state/provenance assertions.

2. **DQA-006 — resolved for the integrated build:** the repaired workspace
   links now allow both `@bnbera/ui` and `@bnbera/web` production builds to
   pass. The web package still has no package-local test files; its behavior is
   covered by the 12 web contract tests in the root QA harness.

3. **DQA-007 — open, P1 browser-evidence limitation:** no `agent-browser`
   executable is available in this worker shell. No browser, authenticated
   state, transaction, payment, custody, deployment, chain, or storage
   evidence is claimed here. Owner: coordinator/QA environment.

The automated Core Marketplace read-model/API contract gates pass locally. The
QA + Deployment + Submission browser sub-gate remains unsigned until the
isolated browser runner is available; this evidence is R1 contract and local
preview HTTP evidence only.

## Evidence level and truthfulness boundary

Evidence level: local source-contract tests, API route invocation tests, static
security assertions, and parser-only synthetic HTTP checks. The fixture-shaped
records use explicit fixture labels and are never described as discovered, live,
healthy, executable, paid, settled, onchain, custody-backed, or Greenfield
published evidence. A successful HTTP response, fixture response, or health
field alone is not treated as proof of an end-to-end capability.

## Feature flags and disable procedure

Keep `coreMarketplace` available for read-only browse/search/filter/detail/
compare; the integrated projection and API contract now pass. Keep
`activationCommerce`, `creatorAltana`, and `evidencePublication` disabled. Keep
all ERC-8183, X402/B402, Greenfield, Altana write, custody, and mainnet paths
disabled until their independent gates pass and the standards lock is verified.

For rollback, remove the QA-only aliases/tests or stop invoking the two probes;
these paths do not mutate runtime behavior. Do not set
`BNBERA_REQUIRE_STATE_MARKERS=0` for release sign-off. Leaving the optional
feature flags and unresolved lock entries unchanged is the safe disable path.

## Handoff status

McClintock did not commit. The coordinator should retain the exact 42/42,
build, and local preview API results above, run the standards check and browser
verification in an environment that permits them, record final exact base/head
SHAs, and create/push the single integration commit. This worker made no claim
of live discovery, activation, payment, custody, deployment, chain, or storage
evidence.

## Coordinator integration addendum

After the worker handoff, the coordinator completed the remaining local checks:

- `pnpm check`: build, lint, and typecheck passed. Its first test phase exposed
  that `@bnbera/ui` had no local test files; the package script was corrected to
  use Vitest's explicit `--passWithNoTests` behavior because UI behavior is
  exercised by the web and cross-package suites.
- `pnpm test`: passed across all workspace projects after that correction.
- Root cross-package QA: 42/42 passed; marketplace: 15/15; web adapter: 4/4.
- The production server API probe passed against
  `http://127.0.0.1:3100/api/marketplace` with an HTTP 200, `empty` read state,
  zero records, and the expected contract version. This is local HTTP evidence.
- Headless Playwright Chromium on ARM64 rendered `/`, marketplace browse, all
  four category routes, `fixture-grid-trader` detail, a two-agent comparison,
  and loading/empty/degraded/error preview states. Every response was HTTP 200,
  body content was non-empty, no framework overlay appeared, and no console or
  page errors were captured. Submitting search `grid` with BSC testnet selected
  navigated to `?q=grid&chainId=97&sort=relevance`. The records were explicitly
  labelled development fixtures; this is browser behavior evidence, not live
  registry, chain, payment, execution, custody, deployment, or storage proof.
- `pnpm ops:standards-check` ran outside the IPC-restricted sandbox and stopped
  fail-closed because `BSC_MAINNET_RPC_URL` was not supplied. No credential or
  endpoint was invented. The unresolved standards-lock gates therefore remain
  disabled and are not release evidence for any optional integration.

DQA-007 is resolved for local rendered Core Marketplace behavior. Deployment
and production-browser evidence remain outside this W0/W1 integration, and the
QA + Deployment + Submission production sub-gate is not claimed as complete.

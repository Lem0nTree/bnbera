{
  "name": "@bnbera/altana-studio-spike",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Phase-zero adapter contract and runner for browser-controlled Altana to Agent Studio validation",
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src test --max-warnings 0",
    "test": "tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "engines": {
    "node": ">=22"
  }
}
